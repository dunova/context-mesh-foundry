# 2026-03-25 测试集压测报告

## 测试集

- 日期：`2026-03-25`
- 清单：`artifacts/testsets/dataset_2026-03-25.json`
- 总条目：`72`
- 来源分布：
  - `codex_session`: `70`
  - `codex_history`: `1`
  - `shell_zsh`: `1`

## 覆盖面

- `search`
- `semantic`
- `save / export / import`
- `serve / viewer API / SSE`
- `health / healthcheck / smoke / e2e_quality_gate`
- `native-scan (rust/go)`
- `benchmarks`
- `smoke_installed_runtime`
- 文档命令合同
- 跨路径一致性矩阵

## 结果摘要

- `save / export / import / semantic`：通过，导入幂等正常。
- `viewer API / SSE`：通过，`/api/health`、`/api/search`、`/api/events` 正常。
- `health / smoke / e2e_quality_gate`：通过，诊断输出可用。
- `native rust / native go`：通过，命中结果与测试集一致。
- `benchmarks`：通过，说明与 CLI/README 一致。
- `installed runtime`：通过；并发场景下 Go native 曾出现 `resource temporarily unavailable`，已在 `context_smoke.py` 增加瞬时重试修复。
- 文档合同：通过，README / ARCHITECTURE / RELEASE_* 与当前实现一致。
- 一致性矩阵：通过，`search / semantic / native / smoke` 链路一致。

## 已确认并已修复

1. 多 agent 并发测试时，`context_smoke.py` 的 Go native 合同检查偶发 `go.mod: resource temporarily unavailable`
   - 已修复：native smoke 合同增加一次瞬时重试。

2. 当前仓库测试集路径会污染日期检索
   - 已修复：`session_index` 当前仓库 meta 过滤加入测试集标记。

## 仍可继续优化

1. 长句 `--literal` 查询未命中
   - 现状更像“严格精确匹配”的设计边界，不是稳定性故障。
   - 若要优化，可加“长句 literal 自动拆锚点”回退。

2. 日期检索的前几条 snippet 仍偏路径占位
   - 功能正确，但可读性一般。
   - 后续可优先抽取“变更概览 / 核心变化 / 最终交付”窗口。
