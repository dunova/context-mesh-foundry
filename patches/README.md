# Patches

This directory is reserved for explicit legacy/third-party patch files when needed.

For now, the recommended path is the scripted helper:
- `../scripts/patch_openviking_semantic_processor.py`
